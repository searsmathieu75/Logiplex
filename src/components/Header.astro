---
import { primaryNav, cta } from '../data/navigation';
import Logo from './Logo.astro';
---

<header class="bg-charbon text-technique sticky top-0 z-50">
  <div class="mx-auto max-w-[88rem] px-6 md:px-12">
    <div class="flex items-center justify-between gap-8 py-5 md:py-6">
      <a href="/" class="hover:text-liaison transition-colors" aria-label="GoWifi, accueil">
        <Logo size="sm" />
      </a>

      <!-- Navigation bureau -->
      <nav aria-label="Navigation principale" class="hidden md:block">
        <ul class="flex items-center gap-8">
          {
            primaryNav.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm font-medium text-technique/80 hover:text-liaison transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
          <li>
            <a
              href={cta.href}
              class="bg-liaison on-liaison text-charbon px-5 py-2.5 text-sm font-semibold hover:bg-liaison/85 transition-colors inline-block"
            >
              {cta.label}
            </a>
          </li>
        </ul>
      </nav>

      <!-- Bouton menu mobile -->
      <button
        type="button"
        id="menu-toggle"
        class="md:hidden text-sm font-semibold uppercase tracking-widest text-technique"
        aria-expanded="false"
        aria-controls="menu-mobile"
      >
        <span data-menu-label>Ouvrir le menu</span>
      </button>
    </div>
  </div>

  <!-- Panneau mobile. Fermé par défaut côté serveur; sans JavaScript il
       n'est pas atteignable, ce qui est acceptable car chaque destination
       reste accessible depuis le pied de page. -->
  <nav
    id="menu-mobile"
    aria-label="Navigation mobile"
    class="md:hidden hidden border-t border-technique/15"
  >
    <ul class="mx-auto max-w-[88rem] px-6 py-6 flex flex-col gap-1">
      {
        primaryNav.map((link) => (
          <li>
            <a
              href={link.href}
              data-menu-link
              class="block py-3 text-xl font-display tracking-tight hover:text-liaison transition-colors"
            >
              {link.label}
            </a>
          </li>
        ))
      }
      <li class="pt-4">
        <a
          href={cta.href}
          data-menu-link
          class="bg-liaison on-liaison text-charbon block px-5 py-3 text-center font-semibold"
        >
          {cta.longLabel}
        </a>
      </li>
    </ul>
  </nav>
</header>
